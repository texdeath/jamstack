import { TopPageComponent } from "@/components/top/TopPageComponent";

/**
 * トップ階層アクセス時に表示されるコンポーネントです。
 */
const Home = () => {
  return (
    <div>
      <h1>skill set</h1>
      <TopPageComponent />
    </div>
  );
};

export default Home;
